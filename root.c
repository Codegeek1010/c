/*Program to finding Squareroot*/
#include<stdio.h>
#include<math.h>
int main(float n)
{
    printf("Enter the number: ");
    scanf("%f",&n);

    printf("Square root of the number is: %f",sqrt(n));

    return 0;
}
